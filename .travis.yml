language: node_js
node_js:
  - 7
deploy:
  provider: pages
  before_script: npm install && npm build
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  local_dir: public/
  on:
    branch: master